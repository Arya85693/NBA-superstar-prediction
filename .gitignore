# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/

# Node / frontend (when you scaffold web/)
web/node_modules/
web/.next/
web/dist/
web/build/
*.local

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Env
.env
.env.*

# Optional: large generated data (uncomment if you do not want CSVs in git)
# data/*.csv

# Market Price state is derived each cycle and published to Supabase (continuity
# lives in the DB / a local run). Keep this churny artifact out of git.
data/player_market_state.csv
